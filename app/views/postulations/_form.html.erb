<%= form_with(model: postulation) do |form| %>
  <% if postulation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(postulation.errors.count, "error") %> prohibited this postulation from being saved:</h2>

      <ul>
        <% postulation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :message, style: "display: block" %>
    <%= form.text_area :message %>
  </div>

  <div>
    <%= form.label :job_offer_id, style: "display: block" %>
    <%= form.text_field :job_offer_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
